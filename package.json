{
  "name": "humanai-trust-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "check:docs": "node scripts/check-doc-references.mjs",
    "smoke:runtime": "node scripts/smoke-runtime.mjs",
    "validate:export": "node scripts/validate-export.mjs",
    "summarize:export": "node scripts/summarize-export.mjs",
    "verify:final": "npm run lint && tsc --noEmit && npm run check:docs && npm run validate:export -- --file data/runs/local-dev/events.jsonl --full-session --expect-condition-id control --expect-event-count 22 --expect-decision-count 10 && npm run summarize:export -- --file data/runs/local-dev/events.jsonl --latest-only && npm run build"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "babel-plugin-react-compiler": "1.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "typescript": "^5"
  }
}
